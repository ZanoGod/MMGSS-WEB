# Myanmar GSS Website




## Refined asset structure

- `css/style.css` is the shared global stylesheet (including layout and home-level refinements).
- `css/premium-carousel.css` and `js/premium-carousel.js` are the single premium carousel implementation.
- Page-specific styles remain in `css/about.css`, `css/process.css`, etc. only when present/needed.
- Removed duplicate legacy image-carousel scripts/styles.
